*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
**/build
**/release
# app/src/release/ is a real source set, not a build output: the Baseline
# Profile plugin commits app/src/release/generated/baselineProfiles/baseline-prof.txt
# there (app/release/ — the AGP APK output dir — stays ignored).
!app/src/release/
/captures
.externalNativeBuild
.cxx
local.properties
.kotlin

# Signing keystores must never be committed
*.jks
*.keystore

# Android Studio Navigation editor temp files
.scratch/

# --- Flutter / Dart ---
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
**/doc/api/
**/ios/Flutter/.last_build_id
app.*.symbols
app.*.map.json

# --- Migration planning docs (temporary, deleted at the end) ---
plans/
# Widget Preview related
.widget_preview/
