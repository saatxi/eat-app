package com.albertferran.eatapp.data.sync

object RemoteConfig {
    // TODO: reemplazar por la URL real del repositorio público de GitHub donde se
    // publique el fichero eatapp.db (owner/repo/branch/ruta).
    const val DATABASE_URL = "https://raw.githubusercontent.com/<OWNER>/<REPO>/<BRANCH>/<PATH>/eatapp.db"
}
