<?xml version="1.0" encoding="utf-8"?>
<!--
    android:updatePeriodMillis is 0 on purpose: the widget is redrawn when the
    app republishes it (every write, and on launch) or when its shuffle button
    is tapped, so a periodic wake-up would only spend battery re-reading the
    same data.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_description"
    android:initialLayout="@layout/eatapp_home_widget"
    android:minHeight="110dp"
    android:minWidth="110dp"
    android:resizeMode="horizontal|vertical"
    android:targetCellHeight="2"
    android:targetCellWidth="2"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
