<?xml version="1.0" encoding="utf-8"?>
<!--
  The window Android shows while the Flutter engine starts, on API 23 to 31:
  the brand's ground with the app's own logo centred on it. Android 12 and
  later draws its own system splash from the launcher icon instead, so this
  file is what the older ones — and anyone with the system splash disabled —
  see.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item
        android:drawable="@drawable/splash_logo"
        android:gravity="center"
        android:height="96dp"
        android:width="96dp" />
</layer-list>
