import 'package:flutter/material.dart';

/// Standard M3 baseline error tones. Shared by every palette and kept
/// independent of the brand seed: error is a semantic red regardless of hue,
/// and a palette that recoloured it would be saying something it doesn't mean.
///
/// Port of the Android app's `Color.kt`.
const Color error10 = Color(0xFF410E0B);
const Color error20 = Color(0xFF601410);
const Color error30 = Color(0xFF8C1D18);
const Color error40 = Color(0xFFB3261E);
const Color error80 = Color(0xFFF2B8B5);
const Color error90 = Color(0xFFF9DEDC);
